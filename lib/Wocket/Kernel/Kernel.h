namespace Wocket::Kernel {
    class Kernel {
    public:
        void setup();
        void think();

    private:
        //uint8_t pin, dutyCycle, dynamicRange;
        //uint16_t frequency;
    };
}

